<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Caats">
        <activity
            android:name=".ui.tutor.TutorReportsActivity"
            android:exported="false"
            android:theme="@style/Theme.Caats" />

        <activity
            android:name=".ui.student.MarkAttendanceActivity"
            android:exported="false" />
        <activity
            android:name=".ui.common.ProfilesActivity"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".ui.common.splashActivity" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->

        <activity
         android:name=".ui.cordinator.CoordinatorActivity"
         android:exported="false" />

        <activity
            android:name=".ui.tutor.AttendanceListActivity"
            android:exported="false" />
        <activity
            android:name=".ui.tutor.TutorDashboardActivity"
            android:exported="false" />
        <activity
            android:name=".ui.student.AttendanceViewActivity"
            android:exported="false" />
        <activity
            android:name=".ui.student.StudentDashboardActivity"
            android:exported="false" />
        <activity
            android:name=".auth.SignupActivity"
            android:exported="false"
            android:windowSoftInputMode="stateVisible" />
        <activity
            android:name=".auth.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Caats">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".utils.FirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>